# Dev: leave VITE_API_URL unset — the Vite proxy forwards /api to the target
# below, keeping the Better Auth session cookie first-party (same origin).
VITE_API_PROXY_TARGET=https://api-production-4f160.up.railway.app
